name: ai-trading-agent
dependencies:
  - python=3.9
  - pip
  - pip:
      - torch
      - torchvision
      - stable-baselines3
      - transformers
      - vaderSentiment
      - optuna
      - streamlit
      - scikit-learn
      - pandas
      - numpy
      - scipy
      - matplotlib
      - jupyter
      - requests # added for API calls (Binance, OANDA)
